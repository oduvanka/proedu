import ReviewsPage from "../../../../components/pages/reviews-page";

const Page = ({ params }) => {
  //const [resultReviews, resultUsers] = Promise.all([fetch(), fetch()]);

  return (
    <div>
      <ReviewsPage />
    </div>
  );
};

export default Page;
